##########################################################
# Data to compose the PUSH command to push the off-card 
# created secure channel session keys to the card

/set-var -g APSD.PUSH.SK.KEYVERSION 22
/set-var -g APSD.PUSH.SK.COUNTERINIT "N/A"
/set-var -g APSD.PUSH.SK.SECDOMIMGNR "N/A"